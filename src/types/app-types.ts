// Define the type for a skill in the about section
export interface Skill {
  name: string;
  component: string;
  style: string;
}

// Define the type for an image in the carousel
export interface SlideImage {
  src: string;
  alt: string;
}

export interface Social {
  url: string;
  name: string;
}
